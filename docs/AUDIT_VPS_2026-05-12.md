# BJHUNT Infrastructure Security Audit - VPS Hostinger 82.25.117.79

**Date** : 2026-05-12  
**Auditeur** : Agent DevSecOps BJHUNT 4 MAX  
**Cible** : `srv1295179` (Hostinger VPS, 8 vCPU / 32 GB RAM, Ubuntu 24.04)  
**Méthode** : SSH audit complet via alias `bjhunt-vps`

---

## 1. Vue d'ensemble infrastructure

### 1.1. Connectivité
| Élément | Statut | Détail |
|---------|--------|--------|
| SSH | OK | root@82.25.117.79:22 (alias `bjhunt-vps`) |
| Kernel | OK | Linux 6.8.0-107-generic |
| Hostname | OK | `srv1295179` |

### 1.2. Docker Stack
- **Conteneurs actifs** : 8/8 (`docker ps -a`)
- **Images** : 23 images, 7 actives, 16 inactives (images E2B custom-envs obsolètes)
- **Volumes** : 5 volumes locaux (bjhunt-postgres-data, bjhunt-redis-data, bjhunt-chats, coolify-db, coolify-redis)
- **Réseaux** : 4 bridges actifs (`bjhunt-net`, `coolify`, `bridge`, `host`)

| Conteneur | Image | Statut | Ports | Healthcheck |
|-----------|-------|--------|-------|-------------|
| `bjhunt-postgres` | pgvector/pgvector:pg17 | Up 3d (healthy) | 127.0.0.1:5432 | pg_isready OK |
| `bjhunt-redis` | redis:7-alpine | Up 3d (healthy) | 127.0.0.1:6379 | redis-cli PING OK |
| `bjhunt-litellm` | ghcr.io/berriai/litellm:main-stable | Up 3d (healthy) | 127.0.0.1:4000 | HTTP liveliness OK |
| `bjhunt-backend` | bjhunt-backend:latest | Up 3d (healthy) | 127.0.0.1:8080 | curl /api/health OK |
| `coolify` | ghcr.io/coollabsio/coolify:latest | Up 3d (healthy) | 127.0.0.1:8000 | - |
| `coolify-realtime` | ghcr.io/coollabsio/coolify-realtime:1.0.13 | Up 3d (healthy) | 127.0.0.1:6001-6002 | - |
| `coolify-db` | postgres:15-alpine | Up 3d (healthy) | 5432/tcp (int) | - |
| `coolify-redis` | redis:7-alpine | Up 3d (healthy) | 6379/tcp (int) | - |

### 1.3. Services système
24 services running via systemd, dont les plus critiques :
- `cloudflared.service` - running, stable
- `docker.service` - running
- `fail2ban.service` - running
- `ssh.service` - running
- `cron.service` - running
- `unattended-upgrades.service` - running (mises à jour automatiques)

---

## 2. Audit Réseau

### 2.1. Ports ouverts (`ss -tlnp`)
```
0.0.0.0:22                  sshd          # Seul port publique
127.0.0.1:5432              docker-proxy  # PostgreSQL bjhunt ❤
127.0.0.1:6379              docker-proxy  # Redis bjhunt ❤
127.0.0.1:4000              docker-proxy  # LiteLLM ❤
127.0.0.1:8080              docker-proxy  # bjhunt-backend ❤
127.0.0.1:8000              docker-proxy  # Coolify ❤
127.0.0.1:6001-6002         docker-proxy  # Coolify Realtime ❤
127.0.0.1:20241             cloudflared   # Cloudflare metrics ❤
127.0.0.53/54:53            systemd-resolve # DNS local
```
**Verdict** : PostgreSQL, Redis, LiteLLM, et backend sont correctement liés à `127.0.0.1`. Seul le SSH (port 22) est exposé sur `0.0.0.0`. Conforme aux specs AGENTS.md.

### 2.2. iptables
- Chaînes UFW présentes dans les tables, mais **UFW n'est pas installé** sur le VPS (`ufw: command not found`).
- Règles Docker natifs actives : `DOCKER-USER`, `DOCKER-FORWARD`, `DOCKER-BRIDGE`.
- Règle de sécurité intéressante dans `DOCKER-USER` :
  ```
  ACCEPT  tcp  127.0.0.0/8   -> 172.16.1.5:8080  # Coolify localhost
  ACCEPT  tcp  10.7.0.0/24   -> 172.16.1.5:8080  # Coolify WireGuard
  DROP    tcp  0.0.0.0/0     -> 172.16.1.5:8080  # Coolify drop public
  ```
  La chaîne `DOCKER` drop également tout trafic non-bridge vers les conteneurs. Bonne pratique.

### 2.3. WireGuard
```
Interface: wg0
  Public key: f92v+Q2cnXjq3WwPTxATnBxppU0xpCpGx3cNDvJn9hk=
  Listening port: 51820
  IP: 10.7.0.1/24
```
**Risque potentiel** : `wg show` ne liste **aucun peer**. Si WireGuard est utilisé pour le mesh E2B/Fly.io/connectivité interne, aucun endpoint pair n'est actuellement configuré. À vérifier si c'est volontaire (phase non encore déployée). Le port 51820 n'est PAS dans la liste des ports ouverts (`ss -tlnp`), ce qui est normal car WireGuard utilise UDP et `ss` sans `-u` ne le montre pas, mais il faut vérifier qu'il n'est pas exposé publiquement (`iptables -L -n | grep 51820`).

### 2.4. Cloudflare Tunnel
- Tunnel ID : `68d75d7f-c39e-415f-ab0e-0b87414ac66c` (nom logique `bjhunt-prod`)
- Version : `2026.3.0`
- Connexions : 4 connexions actives sur les pops CDG (Paris)
- Ingress actif : `api.bjhunt.com` -> `http://localhost:8080`
- Metrics : `127.0.0.1:20241/metrics`
- Fallback : `http_status:404`

**Verdict** : Tunnel opérationnel. Pas d'exposition directe de l'API.

---

## 3. Audit Docker Compose /data/bjhunt-stack

### 3.1. Fichier docker-compose.yml
- `name: bjhunt` - conforme
- **Ports** : tous les services bindés explicitement à `127.0.0.1:<port>:<port>`
- **Healthchecks** : présents sur postgres, redis, litellm, bjhunt-backend
- **Restart Policy** : `unless-stopped` sur tous les services critiques
- **Vols persistants** : `bjhunt-postgres-data`, `bjhunt-redis-data`, `bjhunt-chats`
- **`read_only: true`** sur `bjhunt-backend` (bonne pratique)
- **`no-new-privileges:true`** sur `bjhunt-backend` (bonne pratique)
- **`cap_drop: ALL`** sur `bjhunt-backend` (très bonne pratique)
- **`tmpfs`** pour `/tmp` et `/opt/openclaude/bjhunt-runtime` (limité 128m / 64m)

### 3.2. Fichier .env (review sécurité)
Le fichier `/data/bjhunt-stack/.env` est en **permissions 600 (root only)**. Cependant, il contient **de nombreux secrets en clair** :
- `POSTGRES_PASSWORD`
- `REDIS_PASSWORD`
- `LITELLM_MASTER_KEY`
- `OLLAMA_CLOUD_API_KEY`
- `JWT_SECRET_TICKET`
- `BETTERAUTH_SECRET`
- `E2B_API_KEY` + `E2B_ACCESS_TOKEN`
- `R2_SECRET_ACCESS_KEY`
- `SENTRY_DSN_BACKEND`
- `BJHUNT_RELAY_SECRET`
- `BJHUNT_SECRET_MASTER_KEY`

**Remarque** : `BJHUNT_VAST_API_BASE` et `BJHUNT_VAST_API_KEY` sont encore en valeur placeholder, ce qui indique que l'endpoint vLLM Vast.ai n'est pas encore déployé.

---

## 4. Audit Base de données PostgreSQL (`bjhunt`)

### 4.1. Inventaire tables
- **Total tables** : 82 tables
- **Schéma** : `public`
- **Tables LiteLLM** : 58 tables (préfixe `LiteLLM_`)
- **Tables bjhunt** : 24 tables (BetterAuth, Drizzle, domain)

### 4.2. Row Level Security (RLS)
**14 tables bjhunt avec RLS activé** (conforme AGENTS.md "RLS FORCE sur toutes les tables tenant-scoped") :

| Table | Row Security |
|-------|-------------|
| `messages` | t |
| `attachments` | t |
| `memories` | t |
| `share_links` | t |
| `api_keys` | t |
| `usage_metering` | t |
| `orgs` | t |
| `users` | t |
| `audit_log` | t |
| `org_members` | t |
| `chats` | t |
| `findings` | t |
| `evidence` | t |
| `stream_events` | t |

**⚠ ATTENTION** : Les tables ` LiteLLM_*` ne sont PAS listées avec `rowsecurity = true`. Si le schéma LiteLLM gère ses propres permissions (ce qu'il fait via son UI), c'est acceptable, mais à documenter si des données tenant sont stockées dans ces tables.

### 4.3. Tailles des tables bjhunt
Taille de la base très faible (données de dev / early prod) :
- `stream_events` : 976 kB (la plus grande table bjhunt)
- `audit_log` : 48 kB
- `_prisma_migrations` : 64 kB
- Reste des tables < 16 kB

---

## 5. Audit Clés SSH

Le fichier `~/.ssh/authorized_keys` contient **5 clés SSH** (6 entrées avec doublon) :

| Clé | Type | Commentaire |
|-----|------|-------------|
| ssh-ed25519 | `bjhunt-vps-2026-04-29` | Clé admin principale |
| ssh-ed25519 | `coolify` | Clé déploiement Coolify |
| ssh-ed25519 | `bjhunt-backend-gh-actions-deploy` | CI/CD GitHub Actions |
| ssh-ed25519 | `bjhunt-vps-claudecode-20260508` | Double clé identique |
| ssh-rsa | `#hostinger-managed-key` | Clé RSA Hostinger (legacy) |

**Risque** : Clé RSA Hostinger présente. RSA > 2048-bit mais ed25519 est préférable. La présence d'une clé "managed" par Hostinger peut signifier un accès technique tierce.

---

## 6. Audit Disque & Ressources

| Ressource | Utilisé | Total | % |
|-----------|---------|-------|---|
| Disque `/dev/sda1` | 56G | 387G | 15% |
| Mémoire RAM | 3.7G | 31G | 12% |
| Swap | 0B | 4.0G | 0% |

**Docker System DF** :
- Images : 48.67 GB (16.88 GB récupérables / 34%)
- Build Cache : 33.29 GB (21.15 GB récupérables)
- Conteneurs : 71.41 MB
- Local Volumes : 146 MB

---

## 7. Audit Logs système

### 7.1. Docker (`journalctl -u docker`)
- Derniers logs : 8 mai 2026 (reboot / redeploy du stack)
- Erreurs récurrentes : `failed to validate image signature` sur les images E2B custom-envs (sha256 manifest mismatch) — non bloquant mais bruyant.
- Aucun crash récent.

### 7.2. Cloudflared (`journalctl -u cloudflared`)
- Dernier redémarrage : 8 mai 2026
- Tunnel démarré avec succès
- 4 connexions HTTP/2 établies sur les pops CDG
- Warning : `ICMP proxy feature is disabled` (group ID 0 hors ping_group_range) — non critique
- `TUNNEL_TOKEN` masqué dans les logs (*****)

---

## 8. Cron & Automatisation

Deux jobs cron root identifiés :
1. `0 3 * * * /usr/local/bin/bjhunt-pg-dump.sh` — Backup quotidien PostgreSQL
2. `0 4 * * * /usr/local/bin/bjhunt-stream-events-purge.sh` — Purge des stream events

---

## 9. Score Global & Notation

| Domaine | Note | Commentaire |
|---------|------|-------------|
| Isolation réseau | A | Aucun port DB/service exposé publiquement, SSH seul sur 0.0.0.0 |
| Docker hardening | B+ | `read_only`, `no-new-privileges`, `cap_drop:ALL` sur backend. Manque sur les autres services. |
| Secrets management | C | Fichier `.env` en clair sur disque (root-only). Pas de vault/external secret. |
| Auth SSH | B+ | Ed25519 majoritaire, mais RSA Hostinger legacy présent. 5 clés = à auditer régulièrement. |
| WireGuard | C+ | Interface active mais **aucun peer** configuré. État à clarifier. |
| Cloudflare Tunnel | A | 4 connexions stables, ingress correct. |
| RLS PostgreSQL | B+ | 14/14 tables bjhunt avec RLS. Tables LiteLLM non auditées pour RLS. |
| Backup & Maintenance | B | Cron backup PG présent. Pas de vérification de la restauration testée visible. |
| Mises à jour auto | B | `unattended-upgrades` activé. Pas de reboot automatique planifié (livepatch ?). |
| Monitoring centralisé | C | Sentry configuré, mais pas de monitoring infra visible (pas de node-exporter/Prometheus). |

### Score global infrastructure : **B+**

---

## 10. Top 10 Risques Critiques

| Rang | Risque | Sévérité | Détails |
|------|--------|----------|---------|
| 1 | **Secrets en clair dans `.env`** | 🔴 Critique | Toutes les clés API, DB password, auth secrets sont stockés en texte clair dans `/data/bjhunt-stack/.env`. Si le host est compromis, tous les secrets fuitent instantanément. |
| 2 | **Aucun peer WireGuard configuré** | 🟠 Haut | `wg show` ne liste aucun peer. Si le mesh WireGuard est requis pour le backend/app communication, le tunnel est inactif. Si non requis, supprimer l'interface pour réduire la surface d'attaque. |
| 3 | **Images E2B custom-envs non utilisées** | 🟠 Haut | 10+ images custom-envs (~40GB) ne sont pas utilisées par les conteneurs actifs. Occupent de l'espace disque et augmentent la surface d'attaque (ancien code/sandbox). |
| 4 | **Clé SSH RSA Hostinger legacy** | 🟠 Haut | Clé RSA longue présente avec commentaire `#hostinger-managed-key`. Accès potentiel par Hostinger support. À remplacer ou documenter. |
| 5 | **Backend Docker build local non reproductible** | 🟠 Haut | `bjhunt-backend` construit via `build: context: .` sans tag digest. Pas de CI/CD immuable visible pour l'image backend sur ce stack. |
| 6 | **Pas de monitoring infra (CPU/RAM/Disque)** | 🟡 Moyen | Aucune sonde node-exporter / Prometheus / Datadog détectée. L'alerting sur les ressources est aveugle. |
| 7 | **Build cache Docker énorme (33GB)** | 🟡 Moyen | `docker system df` indique 33.29 GB de build cache. Risque de fuite de données de build (layers contenant potentiellement des secrets) dans le cache. |
| 8 | **UFW absent malgré chaînes iptables UFW** | 🟡 Moyen | Les chaînes `ufw-*` existent mais le binaire `ufw` est manquant. État incohérent. Risque de règles fantômes si UFW est réinstallé plus tard. |
| 9 | **Vérification de backup non prouvée** | 🟡 Moyen | Script `bjhunt-pg-dump.sh` en cron, mais pas de log de succès/échec intégré au monitoring. Pas de restore test visible. |
| 10 | **Coolify expose 127.0.0.1:8000 avec potentiel port-forwarding risqué** | 🟢 Faible | Coolify est accessible en local. Si un port-forwarding SSH est ouvert (`ssh -L`), l'accès admin Coolify peut être exposé. Pas de risque direct, mais à surveiller. |

---

## 11. Recommandations d'action immédiates

### Immédiat (ce jour)
1. **Migrer les secrets vers un vault** : docker secrets (Swarm), HashiCorp Vault, ou au minimum un chiffrement des fichiers `.env` via `sops` / Age. Les secrets ne doivent plus être en texte clair sur disque.
2. **Nettoyer les images Docker mortes** : `docker image prune -a` (ou manuellement supprimer les images E2B custom-envs obsolètes). Libérer ~40GB.
3. **Nettoyer le build cache** : `docker buildx prune -f` pour supprimer les 33GB de cache accumulés.
4. **Vérifier WireGuard** : Confirmer si `wg0` sans peer est intentionnel. Si non requis, arrêter/supprimer l'interface et le service WireGuard.

### Court terme (cette semaine)
5. **Auditer et documenter les clés SSH** : Supprimer la clé RSA Hostinger si non nécessaire, ou la remplacer par une nouvelle clé ed25519 si un accès Hostinger est requis.
6. **Activer le monitoring infrastructure** : Déployer node-exporter + Prometheus (ou agent Datadog/NewRelic) pour surveiller CPU, RAM, disque, IO.
7. **Ajouter un WAF/Rate-limit sur le tunnel** : Cloudflare offre des règles de rate-limiting. Activer sur `api.bjhunt.com` pour prévenir les abus.
8. **Configurer un alerting backup** : Le script `bjhunt-pg-dump.sh` doit émettre une alerte (Sentry, mail, webhook) en cas d'échec.

### Moyen terme (ce mois-ci)
9. **Hardening conteneures restants** : Appliquer `read_only`, `no-new-privileges`, `cap_drop: ALL` aux conteneurs postgres, redis, litellm (actuellement absents).
10. **Mise en place de Docker Content Trust / Image signature** : Actuellement désactivé (erreurs `failed to validate image signature` pour les images E2B). Pour les images critiques, pull avec signature Notation/Cosign.
11. **Tests de restore** : Effectuer un test de restauration du dump PostgreSQL sur une instance locale pour valider l'intégrité des backups.

---

## 12. Glossaire des processus/services

| Service | Type | État | Utilisation | Santé |
|---------|------|------|-------------|-------|
| `bjhunt-backend` | Container Docker | Running | API backend Hono4/Bun | Healthy |
| `bjhunt-postgres` | Container Docker | Running | BDD principale (pg17+pgvector) | Healthy |
| `bjhunt-redis` | Container Docker | Running | Cache / Queue / Session | Healthy |
| `bjhunt-litellm` | Container Docker | Running | Proxy LLM / Key management | Healthy |
| `coolify` | Container Docker | Running | PaaS interne | Healthy |
| `coolify-realtime` | Container Docker | Running | Soketi WebSocket | Healthy |
| `coolify-db` | Container Docker | Running | BDD Coolify (postgres 15) | Healthy |
| `coolify-redis` | Container Docker | Running | Cache Coolify | Healthy |
| `cloudflared` | Systemd | Running | Tunnel Cloudflare ingress | Active (4 connexions) |
| `fail2ban` | Systemd | Running | Brute-force protection | Active |
| `sshd` | Systemd | Running | Accès SSH | Active |
| `docker` | Systemd | Running | Runtime conteneurs | Active |
| `cron` | Systemd | Running | Tâches planifiées (backup/purge) | Active |
| `unattended-upgrades` | Systemd | Running | Patch management auto | Active |
| `wg0` | Interface Kernel | Up | WireGuard VPN | Up (sans peer) |

---

*Rapport généré par BJHUNT 4 MAX - Audit DevSecOps automatisé.*
